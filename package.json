{
    "name": "hiver-task-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -r frontend/dist/* dist/",
        "install-frontend": "cd frontend && npm install"
    },
    "engines": {
        "node": "22.x"
    }
}